export interface SummarySettings {
    currency: 'YER' | 'SAR' | 'USD';
      rate: string;
        floors: string;
        }

        export interface SummaryData {
          buildingArea: number;
            prelim: any;
              struct: any;
                finishes: any;
                  prices: any;
                  }
